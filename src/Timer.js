
class Timer {
  constructor(name) {
  
    this.name = name; // name of timer
    this.time = 0; // time in milliseconds
    this.isRunning = false // timer on or off
    
  }

}

export default Timer

